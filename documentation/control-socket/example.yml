# Runtime control over a unix socket: start/stop/status on demand.
control:
  socket: {{SOCKET}}
processes:
  - name: app
    command: /usr/local/bin/app
    args: ["300"]
    startup: disabled
    on-failure: ignore
