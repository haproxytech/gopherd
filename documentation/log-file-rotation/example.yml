# File log target with size-based rotation and retention.
# Log-targets require log-capture (capture is off by default).
log-capture: true
processes:
  - name: app
    command: /usr/local/bin/app
    args: ["300"]
    on-failure: shutdown
log-targets:
  app-file:
    type: file
    location: /var/log/app.log
    services: [app]
    max-size: 10MiB
    max-files: 5
    compress: true
